<%= form_for(@admin_unit) do |f| %>
  <% if @admin_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_unit.errors.count, "error") %> prohibited this admin_unit from being saved:</h2>

      <ul>
      <% @admin_unit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :symbol %><br />
    <%= f.text_field :symbol %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
